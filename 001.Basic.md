# Python Must know

## Get the kernel interpreter path.
import sys
print(sys.executable)

## Create a conda environment and activate it and then install tensorFlow, tensorflow doest not support <= 3.12
conda create -n tf_env python=3.10
conda activate tf_env
pip install tensorflow
